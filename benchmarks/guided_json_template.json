{
    "$defs": {
        "PickedLine": {
            "description": "Picked line from the sonnet dataset.",
            "properties": {
                "line_type": {
                    "description": "Type of the poem line.",
                    "title": "Line Type",
                    "type": "string"
                },
                "line": {
                    "description": "A random poem line.",
                    "title": "Line",
                    "type": "string"
                }
            },
            "required": ["line_type", "line"],
            "title": "PickedLine",
            "type": "object"
        }
    },
    "description": "A list of picked poem lines anticipating a user looking for the subset of poem lines from input data",
    "properties": {
        "lines": {
            "description": "Picked poem lines for the given input.",
            "items": {"$ref": "#/$defs/PickedLine"},
            "title": "Lines",
            "type": "array",
            "minItems": 1
        }
    },
    "required": ["lines"],
    "title": "Picks",
    "type": "object"
}